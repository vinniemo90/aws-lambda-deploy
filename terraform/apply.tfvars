env = "prod"

aws_region = "us-east-1"

lambda_deployment_pkg = "lambda-deployment-pkg.zip"

lambda_function_name = "lambda-function"

lambda_handler = "lambda-function.lambda_handler"

lambda_runtime = "python3.6"

lambda_description = "Lambda function description"

lambda_environment_variables = {
    ENV_VAR         = "test"
}

lambda_timeout = "60"

lambda_memory_size = "128"